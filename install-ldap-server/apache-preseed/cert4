[server]
name=cert4.students

[webmaster]
email = z.jiricek@ubuntu.local

[features]
apache_ldap=True

[groups]
allow = "cn=cert4,ou=Group,dc=ubuntu,dc=local; cn=teacher,ou=Group,dc=ubuntu,dc=local"

